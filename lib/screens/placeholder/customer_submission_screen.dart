
import 'package:flutter/material.dart';

class CustomerSubmissionScreen extends StatelessWidget {
  const CustomerSubmissionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Customer Submission Screen')),
      body: const Center(child: Text('Customer Submission Screen')),
    );
  }
}
